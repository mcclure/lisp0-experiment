[package]
name = "lisp0"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1.0.95"
char_reader = "0.1.1"
clap = { version = "4.0.*" , features = ["derive"] }

[features]
debug-gc=[] # Print *** messages when GC occurs