# Convoluted expressions to test reader edge cases. (LISP reader)
# Expect: (("z"), ("a", ("b", "c"), ("d", "e", "f", "g", "h", "i", ("j"), ("k"), ("l"), ("m"))))

(z)
(
a # comment
(b c) # comment
( # comment
 d #comment
    e
    f \
    g
    h \ # comment
    i
    (j \
    )
    ( \
    k
    )
    ( \
# comment
    l
    )

    ( \

    # comment

    m
    )
)
)
